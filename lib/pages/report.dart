import 'package:flutter/material.dart';

class Report extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(child: Center(child: Text('this is report')));
  }
}

// a report on how much callory was burned
// learn graphs and illustrations
//goals
// chages in repect to goals
// enter data to redord storage
// add an image to track
