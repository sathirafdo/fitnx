// has timings and everything goi8ng with the execiose
//pause stop next button
// learn to add gifs
// auto transition after set time
import 'package:flutter/material.dart';

class ScheduleRun extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('text here')),
    );
  }
}
